{
    "content_fields": [
        [
            null,
            null,
            [
                {
                    "desc": "**Required.** The encryption algorithm the key in this event is to be used with.",
                    "key": "algorithm",
                    "required": true,
                    "title": "string"
                },
                {
                    "desc": "**Required.** The room where the key is used.",
                    "key": "room_id",
                    "required": true,
                    "title": "string"
                },
                {
                    "desc": "**Required.** The Curve25519 key of the device which initiated the session originally.",
                    "key": "sender_key",
                    "required": true,
                    "title": "string"
                },
                {
                    "desc": "**Required.** The ID of the session that the key is for.",
                    "key": "session_id",
                    "required": true,
                    "title": "string"
                },
                {
                    "desc": "**Required.** The key to be exchanged.",
                    "key": "session_key",
                    "required": true,
                    "title": "string"
                },
                {
                    "desc": "**Required.** The Ed25519 key of the device which initiated the session originally.\nIt is 'claimed' because the receiving device has no way to tell that the\noriginal room_key actually came from a device which owns the private part of\nthis key unless they have done device verification.",
                    "key": "sender_claimed_ed25519_key",
                    "required": true,
                    "title": "string"
                },
                {
                    "desc": "**Required.** Chain of Curve25519 keys. It starts out empty, but each time the\nkey is forwarded to another device, the previous sender in the chain is added\nto the end of the list. For example, if the key is forwarded from A to B to\nC, this field is empty between A and B, and contains A's Curve25519 key between\nB and C.",
                    "key": "forwarding_curve25519_key_chain",
                    "required": true,
                    "title": "[string]"
                }
            ]
        ]
    ],
    "desc": "This event type is used to forward keys for end-to-end encryption. Typically\nit is encrypted as an ``m.room.encrypted`` event, then sent as a `to-device`_\nevent.",
    "msgtype": null,
    "title": null,
    "type": "m.forwarded_room_key",
    "type_with_msgtype": null,
    "typeof": "",
    "typeof_info": ""
}