{
    "content_fields": [
        [
            null,
            null,
            [
                {
                    "desc": "**Required.** An opaque identifier for the verification process. Must be the same as\nthe one used for the ``m.key.verification.start`` message.",
                    "key": "transaction_id",
                    "required": true,
                    "title": "string"
                },
                {
                    "desc": "**Required.** The key agreement protocol the device is choosing to use, out of the\noptions in the ``m.key.verification.start`` message.",
                    "key": "key_agreement_protocol",
                    "required": true,
                    "title": "string"
                },
                {
                    "desc": "**Required.** The hash method the device is choosing to use, out of the options in\nthe ``m.key.verification.start`` message.",
                    "key": "hash",
                    "required": true,
                    "title": "string"
                },
                {
                    "desc": "**Required.** The message authentication code the device is choosing to use, out of\nthe options in the ``m.key.verification.start`` message.",
                    "key": "message_authentication_code",
                    "required": true,
                    "title": "string"
                },
                {
                    "desc": "**Required.** The SAS methods both devices involved in the verification process\nunderstand. Must be a subset of the options in the ``m.key.verification.start``\nmessage. One of: [\"decimal\", \"emoji\"]",
                    "key": "short_authentication_string",
                    "required": true,
                    "title": "[enum]"
                },
                {
                    "desc": "**Required.** The hash (encoded as unpadded base64) of the concatenation of the device's\nephemeral public key (encoded as unpadded base64) and the canonical JSON\nrepresentation of the ``m.key.verification.start`` message.",
                    "key": "commitment",
                    "required": true,
                    "title": "string"
                }
            ]
        ]
    ],
    "desc": "Accepts a previously sent ``m.key.verification.start`` message. Typically sent as a\n`to-device`_ event.",
    "msgtype": null,
    "title": null,
    "type": "m.key.verification.accept",
    "type_with_msgtype": null,
    "typeof": "",
    "typeof_info": ""
}