{
    "content_fields": [
        [
            null,
            null,
            [
                {
                    "desc": "**Required.** An opaque identifier for the verification process. Must be the same as\nthe one used for the ``m.key.verification.start`` message.",
                    "key": "transaction_id",
                    "required": true,
                    "title": "string"
                },
                {
                    "desc": "**Required.** A map of the key ID to the MAC of the key, using the algorithm in the\nverification process. The MAC is encoded as unpadded base64.",
                    "key": "mac",
                    "required": true,
                    "title": "{string: string}"
                },
                {
                    "desc": "**Required.** The MAC of the comma-separated, sorted, list of key IDs given in the ``mac``\nproperty, encoded as unpadded base64.",
                    "key": "keys",
                    "required": true,
                    "title": "string"
                }
            ]
        ]
    ],
    "desc": "Sends the MAC of a device's key to the partner device. Typically sent as a\n`to-device`_ event.",
    "msgtype": null,
    "title": null,
    "type": "m.key.verification.mac",
    "type_with_msgtype": null,
    "typeof": "",
    "typeof_info": ""
}