{
    "__meta": {
        "base": "/.well-known",
        "endpoints": [
            {
                "deprecated": false,
                "desc": "Gets information about the delegated server for server-server communication\nbetween Matrix homeservers. Servers should follow 30x redirects, carefully\navoiding redirect loops, and use normal X.509 certificate validation.",
                "example": {
                    "req": "GET /.well-known/matrix/server HTTP/1.1\n\n"
                },
                "method": "GET",
                "path": "/.well-known/matrix/server",
                "rate_limited": false,
                "req_body_tables": [],
                "req_param_by_loc": {},
                "requires_auth": false,
                "res_headers": null,
                "res_tables": [
                    [
                        null,
                        null,
                        [
                            {
                                "desc": "The server name to delegate server-server communciations to, with optional\nport. The delegated server name uses the same grammar as\n`server names in the appendices <../appendices.html#server-name>`_.",
                                "key": "m.server",
                                "required": false,
                                "title": "string"
                            }
                        ]
                    ]
                ],
                "responses": [
                    {
                        "code": 200,
                        "description": "The delegated server information. The ``Content-Type`` for this response SHOULD be ``application/json``, however servers parsing the response should assume that the body is JSON regardless of type. Failures parsing the JSON or invalid data provided in the resulting parsed JSON should not result in discovery failure - consult the server discovery process for information on how to continue.",
                        "example": "{\n  \"m.server\": \"delegated.example.com:1234\"\n}"
                    }
                ],
                "title": "Gets information about the delegated server for server-server communication."
            }
        ],
        "group": "wellknown_ss"
    },
    "basePath": "/.well-known",
    "host": "localhost:443",
    "info": {
        "title": "Matrix Federation Server Discovery API",
        "version": "1.0.0"
    },
    "paths": {
        "/matrix/server": {
            "get": {
                "description": "Gets information about the delegated server for server-server communication\nbetween Matrix homeservers. Servers should follow 30x redirects, carefully\navoiding redirect loops, and use normal X.509 certificate validation.",
                "operationId": "getWellKnown",
                "responses": {
                    "200": {
                        "description": "The delegated server information. The ``Content-Type`` for this response SHOULD be ``application/json``, however servers parsing the response should assume that the body is JSON regardless of type. Failures parsing the JSON or invalid data provided in the resulting parsed JSON should not result in discovery failure - consult the server discovery process for information on how to continue.",
                        "examples": {
                            "application/json": {
                                "m.server": "delegated.example.com:1234"
                            }
                        },
                        "schema": {
                            "properties": {
                                "m.server": {
                                    "description": "The server name to delegate server-server communciations to, with optional\nport. The delegated server name uses the same grammar as\n`server names in the appendices <../appendices.html#server-name>`_.",
                                    "type": "string"
                                }
                            },
                            "type": "object"
                        }
                    }
                },
                "summary": "Gets information about the delegated server for server-server communication."
            }
        }
    },
    "produces": [
        "application/json"
    ],
    "schemes": [
        "https"
    ],
    "swagger": "2.0"
}