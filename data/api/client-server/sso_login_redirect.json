{
    "__meta": {
        "base": "/_matrix/client/%CLIENT_MAJOR_VERSION%",
        "endpoints": [
            {
                "deprecated": false,
                "desc": "A web-based Matrix client should instruct the user's browser to\nnavigate to this endpoint in order to log in via SSO.\n\nThe server MUST respond with an HTTP redirect to the SSO interface.",
                "example": {
                    "req": "GET /_matrix/client/%CLIENT_MAJOR_VERSION%/login/sso/redirect HTTP/1.1\n\n"
                },
                "method": "GET",
                "path": "/_matrix/client/%CLIENT_MAJOR_VERSION%/login/sso/redirect",
                "rate_limited": false,
                "req_body_tables": [],
                "req_param_by_loc": {
                    "query": [
                        {
                            "desc": "**Required.** URI to which the user will be redirected after the homeserver has\nauthenticated the user with SSO.",
                            "key": "redirectUrl",
                            "required": false,
                            "title": "string"
                        }
                    ]
                },
                "requires_auth": false,
                "res_headers": null,
                "res_tables": [],
                "responses": [
                    {
                        "code": 302,
                        "description": "A redirect to the SSO interface.",
                        "example": null
                    }
                ],
                "title": "Redirect the user's browser to the SSO interface."
            }
        ],
        "group": "sso_login_redirect_cs"
    },
    "basePath": "/_matrix/client/%CLIENT_MAJOR_VERSION%",
    "host": "localhost:8008",
    "info": {
        "title": "Matrix Client-Server SSO Login API",
        "version": "1.0.0"
    },
    "paths": {
        "/login/sso/redirect": {
            "get": {
                "description": "A web-based Matrix client should instruct the user's browser to\nnavigate to this endpoint in order to log in via SSO.\n\nThe server MUST respond with an HTTP redirect to the SSO interface.",
                "operationId": "redirectToSSO",
                "parameters": [
                    {
                        "description": "URI to which the user will be redirected after the homeserver has\nauthenticated the user with SSO.",
                        "in": "query",
                        "name": "redirectUrl",
                        "required": true,
                        "type": "string"
                    }
                ],
                "responses": {
                    "302": {
                        "description": "A redirect to the SSO interface.",
                        "headers": {
                            "Location": {
                                "type": "string"
                            }
                        }
                    }
                },
                "summary": "Redirect the user's browser to the SSO interface."
            }
        }
    },
    "schemes": [
        "https",
        "http"
    ],
    "swagger": "2.0"
}